#!/usr/bin/env python3
"""Run from the repository root; --help is safe and does not fit any model."""

from pareto_weight_calibration.model_tournament import main

if __name__ == "__main__":
  main()
