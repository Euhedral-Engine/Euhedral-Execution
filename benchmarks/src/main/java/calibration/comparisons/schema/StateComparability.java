package calibration.comparisons.schema;

/// Analysis-only classification of policy-conditioned runtime state similarity.
public enum StateComparability {
    STATE_COMPARABLE,
    STATE_SHIFTED,
    STATE_DIVERGENT
}
