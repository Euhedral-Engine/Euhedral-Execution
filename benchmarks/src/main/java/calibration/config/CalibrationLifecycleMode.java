package calibration.config;

/// Defines whether calibration measurement windows restart or preserve the physical scheduler trajectory.
public enum CalibrationLifecycleMode {
    RESET,
    CONTINUOUS
}
