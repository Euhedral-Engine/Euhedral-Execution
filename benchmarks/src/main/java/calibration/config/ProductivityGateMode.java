package calibration.config;

/// Benchmark-only control for mapping the physical productivity-gate response independently of its threshold.
public enum ProductivityGateMode {
    AUTO,
    FORCE_OFF,
    FORCE_ON
}
